<!-- Generated by Orchestra — do not edit by hand. -->
<!-- Codebase: orch -->
<!-- Direction: orchestra/pilot.md -->
<!-- Context: orchestra/.guide.md -->

# Orchestra Guide — orch

This hidden file is the durable background-context document for this codebase.
Read `orchestra/pilot.md` first for direction and operating rules. Use this file
for repo context, preserved conventions, imported guidance, and active work state.

## Identity

- Codebase: `orch`
- Managed by: Orchestra
- Control plane: `orchestra/controls/`
- Primary entrypoint: `orchestra/pilot.md`

## Stack

- **orch** — backend

## Commands

- `orchestra sync orch`
- `orchestra sync orch --dry-run`
- `orchestra status`
- `orchestra diff orch`
- `orchestra doctor`

## Task Sync

- The `<!-- orchestra:tasks -->` block rendered in managed provider files is the canonical editable task surface.
- Edit that block in any managed file to create or update tasks.
- Orchestra writes those changes back into the registry and re-renders the same task state across the other provider files.
- Use status `done` to close work instead of deleting rows.

## Conventions

- Always include validation steps and remaining risks.
- Always state the active phase before implementing.
- Do not assume feature parity for subagents/skills/config formats.
- Do not let one platform drift from canonical phase definitions.
- Do not manually maintain rendered agent files.
- Do not overwrite untracked files without explicit force control.
- Never clobber non-Orchestra-owned files by default.
- Never delete or overwrite non-Orchestra-managed files by default.
- Never introduce destructive file behavior by default.
- Never partially apply multi-command blocks.
- Never perform destructive deletes for user-owned files.
- Never use `unwrap()`/`expect()` in library crates.
- Status must match real filesystem state.

## Notes

- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-cross-agent-sync/SKILL.md] # Done Criteria
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-cross-agent-sync/SKILL.md] # Guardrails
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-cross-agent-sync/SKILL.md] # Objective
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-cross-agent-sync/SKILL.md] # Procedure
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-cross-agent-sync/SKILL.md] # Use This Skill When
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-daemon-watcher/SKILL.md] # Done Criteria
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-daemon-watcher/SKILL.md] # Guardrails
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-daemon-watcher/SKILL.md] # Objective
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-daemon-watcher/SKILL.md] # Procedure
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-daemon-watcher/SKILL.md] # Use This Skill When
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-harness-alignment/SKILL.md] # Done Criteria
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-harness-alignment/SKILL.md] # Guardrails
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-harness-alignment/SKILL.md] # Objective
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-harness-alignment/SKILL.md] # Procedure
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-harness-alignment/SKILL.md] # Use This Skill When
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-registry-foundation/SKILL.md] # Done Criteria
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-registry-foundation/SKILL.md] # Guardrails
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-registry-foundation/SKILL.md] # Objective
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-registry-foundation/SKILL.md] # Procedure
- [legacy:<workspace-root>/AGENT/.agents/skills/orchestra-registry-foundation/SKILL.md] # Use This Skill When

## Active Tasks

Edit this block in any managed Orchestra file to add or update tasks. After save,
the daemon syncs the registry and propagates the same task state to every provider file.
Use status `done` to close work instead of deleting rows.

<!-- orchestra:tasks -->

| ID                  | Title | Status       | Description |
| ------------------- | ----- | ------------ | ----------- | -------------------- | --- |
| <!-- Add rows like: | T-001 | Example task | pending     | optional description | --> |

<!-- /orchestra:tasks -->

## Subagent Delegation

When a task is large or benefits from specialisation, delegate to subagents:

- **Implementation work** — use the `orchestra-worker` subagent (Claude: `.claude/agents/orchestra-worker.md`; other providers: create a focused subtask).
- **Code review** — use the `orchestra-reviewer` subagent (Claude: `.claude/agents/orchestra-reviewer.md`; other providers: self-review or request review).
- **Research / exploration** — create a read-only subtask; avoid writes until the approach is validated.
- Keep delegated work scoped to a single concern per subagent invocation.
- Report delegation outcomes through the Orchestra writeback protocol.

## Final Rule

Use `orchestra/pilot.md` for direction. Use this file for durable context.
