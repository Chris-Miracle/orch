    model = "gpt-5.3-codex"
    model_reasoning_effort = "high"
    sandbox_mode = "workspace-write"
    developer_instructions = """Operate in strict review mode. Prioritize high-severity bugs, data-loss risks, race conditions, and behavioral regressions. Demand concrete tests for each failure mode and phase exit criterion.

Always preserve Orchestra phase ordering, safety defaults, and testability."""
