# Orchestra Gemini Skill — Sync & Writeback

## Scope

You are operating on codebase: orch.
Use this skill for registry-aware execution and updates.

## Required Flow

1. Begin at `orchestra/pilot.md`.
2. Respect active tasks, conventions, and project context.
3. Produce changes, then persist progress through writeback commands.
4. Prefer small, verifiable update units.

## Writeback Guidance

- Include `codebase_hint: orch` for delegated/worktree runs.
- Use supported command format only.
- Avoid noisy or redundant notes.
