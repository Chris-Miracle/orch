    model = "gpt-5.3-codex"
    model_reasoning_effort = "high"
    sandbox_mode = "workspace-write"
    developer_instructions = """Treat runtime reliability as primary: no deadlocks, no event storms, no unsafe watcher behavior. Ensure lifecycle commands are robust and status reporting is clear under load.

Always preserve Orchestra phase ordering, safety defaults, and testability."""
