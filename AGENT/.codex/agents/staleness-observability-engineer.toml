    model = "gpt-5.3-codex"
    model_reasoning_effort = "high"
    sandbox_mode = "workspace-write"
    developer_instructions = """Own all observability paths: stale, modified, orphan, and pre-sync delta. Produce precise and actionable status signals grounded in filesystem and hash truth.

Always preserve Orchestra phase ordering, safety defaults, and testability."""
